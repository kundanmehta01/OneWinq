import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import toast from 'react-hot-toast'; import AuthLayout from '../../components/auth/AuthLayout.jsx'; import { Button, Input } from '../../components/common/UI.jsx'; import { authService } from '../../services/authService.js';
export default function ForgotPassword() { const [target, setTarget] = useState(''); const navigate = useNavigate(); const submit = async (e) => { e.preventDefault(); try { await authService.sendVerification({ target, type: 'PASSWORD_RESET' }); sessionStorage.setItem('reset_target', target); toast.success('Reset code sent'); navigate('/reset-password'); } catch (err) { toast.error(err.message); } }; return <AuthLayout title="Reset your password" subtitle="We will send a verification code to your email or phone."><form onSubmit={submit}><Input label="Email or phone" value={target} onChange={(e) => setTarget(e.target.value)} required /><Button>Send code</Button></form></AuthLayout>; }

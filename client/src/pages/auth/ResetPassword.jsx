import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import toast from 'react-hot-toast'; import AuthLayout from '../../components/auth/AuthLayout.jsx'; import { Button, Input } from '../../components/common/UI.jsx'; import { authService } from '../../services/authService.js';
export default function ResetPassword() { const [form, setForm] = useState({ code: '', newPassword: '' }); const navigate = useNavigate(); const submit = async (e) => { e.preventDefault(); try { await authService.resetPassword({ target: sessionStorage.getItem('reset_target'), ...form }); toast.success('Password updated'); navigate('/login'); } catch (err) { toast.error(err.message); } }; return <AuthLayout title="Choose a new password" subtitle="Use at least eight characters."><form onSubmit={submit}><Input label="Verification code" value={form.code} onChange={(e) => setForm({ ...form, code: e.target.value })} required /><Input label="New password" type="password" minLength="8" value={form.newPassword} onChange={(e) => setForm({ ...form, newPassword: e.target.value })} required /><Button>Reset password</Button></form></AuthLayout>; }

import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import toast from 'react-hot-toast'; import AuthLayout from '../../components/auth/AuthLayout.jsx'; import { Button, Input } from '../../components/common/UI.jsx'; import { authService } from '../../services/authService.js'; import { useAuth } from '../../context/AuthContext.jsx';
export default function Login() { const [form, setForm] = useState({ identifier: '', password: '' }); const [busy, setBusy] = useState(false); const navigate = useNavigate(); const { saveSession } = useAuth(); const submit = async (e) => { e.preventDefault(); setBusy(true); try { const result = await authService.login(form); saveSession(result.data); toast.success('Welcome back'); navigate('/dashboard'); } catch (err) { toast.error(err.message); } finally { setBusy(false); } }; return <AuthLayout title="Welcome back" subtitle="Sign in to manage your professional presence." footer={<>New to OneWinq? <Link to="/signup">Create an account</Link></>}><form onSubmit={submit}><Input label="Email or phone" value={form.identifier} onChange={(e) => setForm({ ...form, identifier: e.target.value })} required /><Input label="Password" type="password" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} required /><Link className="text-link" to="/forgot-password">Forgot password?</Link><Button disabled={busy}>{busy ? 'Signing in…' : 'Sign in'}</Button></form></AuthLayout>; }

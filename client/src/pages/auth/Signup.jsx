import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import toast from 'react-hot-toast'; import AuthLayout from '../../components/auth/AuthLayout.jsx'; import { Button, Input } from '../../components/common/UI.jsx'; import { authService } from '../../services/authService.js';
export default function Signup() { const [form, setForm] = useState({ target: '', password: '' }); const [busy, setBusy] = useState(false); const navigate = useNavigate(); const submit = async (e) => { e.preventDefault(); setBusy(true); try { const type = form.target.includes('@') ? 'EMAIL_VERIFY' : 'PHONE_VERIFY'; await authService.sendVerification({ target: form.target, type }); sessionStorage.setItem('signup_target', form.target); sessionStorage.setItem('signup_password', form.password); sessionStorage.setItem('verification_type', type); toast.success('Verification code sent'); navigate('/verify'); } catch (err) { toast.error(err.message); } finally { setBusy(false); } }; return <AuthLayout title="Create your account" subtitle="Start building your professional identity." footer={<>Already registered? <Link to="/login">Sign in</Link></>}><form onSubmit={submit}><Input label="Email or phone" placeholder="you@company.com" value={form.target} onChange={(e) => setForm({ ...form, target: e.target.value })} required /><Input label="Password" type="password" minLength="8" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} required /><Button disabled={busy}>{busy ? 'Sending code…' : 'Continue'}</Button></form></AuthLayout>; }
